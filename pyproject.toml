[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"




[project]
name = "MetaDeck"
version = "3.0.0"
dependencies = [
]

[tool.setuptools]
packages = ["metadeck"]
package-dir = { "metadeck" = "src/py" }